#!/bin/sh

xsetroot -name "$(sb-mpd)$(sb-netspeed) $(sb-cpu)$(sb-temp)$(sb-memory) $(sb-vol) $(sb-date)"
#echo "$(sb-mpd)$(sb-netspeed) $(sb-cpu)$(sb-temp)$(sb-memory) $(sb-vol) $(sb-date)"
# echo "\ue0b0 \u00b1 \ue0a0 \u27a6 \u2718 \u26a1 \u2699"
